train_loader:
  _target_: PolyDiff.test.run_test.make_dummy_loader
  batch_size: 4
  in_features: ${model.in_features}
  out_features: ${model.out_features}
  n_samples: 100

val_loader: ${data.train_loader}